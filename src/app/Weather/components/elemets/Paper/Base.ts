import { animated } from 'react-spring';
import styled from 'styled-components';

export const Paper = styled(animated.div)`
    padding: 24px 18px;
    border-radius: 8px;
    box-shadow: rgba(17, 12, 46, 0.15) 0px 48px 100px 0px;
`;

export default Paper;
